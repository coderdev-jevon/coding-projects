1. Create the class PasswordManager with all of its attributes and functions
2. Create main function and run it to start user interaction
3. Create menu loop to allow users to select and access, modify the data inside password manager
