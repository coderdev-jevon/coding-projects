1. Create folder structure for the project
2. Create a feature on showing all folders and files in the notes folder
3. For the folders, create a feature to allow user to see what's inside the folder
4. Then allow users to add new file / folder, first specifiy the path, and add it, make the path to appear on the url
5. Create feature to allow user to type in the file and save it